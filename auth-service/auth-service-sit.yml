digital-bank:
  service:
    runtime-profile: sit

auth:
  jwt:
    audiences:
      - api-gateway
      - customer-service
      - account-service
      - ledger-service
      - transaction-service
      - payment-service
      - mfa-service
    scopes:
      - admin.internal
      - mfa.internal
      - payment.internal
      - transfer.internal
  identity:
    fixture:
      username: transfer-orchestrator
