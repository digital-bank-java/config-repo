digital-bank:
  service:
    runtime-profile: sit

auth:
  jwt:
    scopes:
      - admin.internal
      - mfa.internal
      - payment.internal
      - transfer.internal
  identity:
    fixture:
      username: transfer-orchestrator
